Problem:
{{QUESTION}}

Create a reasoning trace as STRICT JSON:
{
  "steps": [
    {"id": 1, "type": "facet", "text": "..."}
  ],
  "answer": "..."
}

Task family guidance:
{{FAMILY_GUIDANCE}}

Rules for steps:
- Use one short atomic claim or local inference per step.
- Give each step exactly one new piece of information.
- Prefer types such as premise, facet, inference, constraint, or comparison.
- You may include intermediate values if they help.
- Do not paraphrase the final answer inside the earlier steps.
- Avoid summary restatements; each later step should depend on earlier steps.
- Put the final answer only in the "answer" field.
- Return JSON only.
